.DS_Store
node_modules
/build
/package
.env
.env.*

# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock

/target

.dfx

# Claude Code git worktrees: nested checkouts of this repo. Prettier reads
# .prettierignore/.gitignore but not .git/info/exclude (where these live), so
# exclude them here to avoid linting/formatting in-progress files from other branches.
.claude/worktrees/
